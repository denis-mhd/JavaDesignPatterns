package design.patterns.structural.adapter;


//Adapter design pattern acts as a wrapper between two objects. It catches calls for one object and transform them to format and interface
//recognizable by the second object
public interface Adapter {

    void request();
}
